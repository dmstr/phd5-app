# Test stack settings

COMPOSE_PROJECT_NAME=test-ee
COMPOSE_FILE=../docker-compose.yml:./docker-compose.test.yml

# This should be the same image as specified in .env (project root)
STACK_PHP_IMAGE=local/phd